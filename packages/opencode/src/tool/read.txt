Read a file from the filesystem.

Assumes any user-provided path is valid. Returns an error if file doesn't exist. Supports images and PDFs. Cannot read binary files. Lines longer than 2000 characters are truncated.

Speculatively read multiple files as a batch when potentially useful.
