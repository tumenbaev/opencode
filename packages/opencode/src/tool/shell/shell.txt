Always use the `workdir` parameter instead of `cd` commands.

Use `${tmp}` for temporary work outside the workspace - it's already been created, exists, and is pre-approved for external directory access.
