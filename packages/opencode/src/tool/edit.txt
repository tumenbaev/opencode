Replace `oldString` with `newString` in a file.

- Always Read the file before editing.
- `oldString` must match exactly once. If multiple matches exist, add more surrounding context or set `replaceAll` to true.
- Preserve all whitespace and indentation exactly as in the file.
- Prefer editing existing files over creating new ones.
